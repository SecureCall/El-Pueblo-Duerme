
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to the games collection
    // This is for development purposes and should be secured for production
    match /games/{gameId} {
      allow read, write: if true;
    }
  }
}
