rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for development purposes.
    // This is insecure and should be changed for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
