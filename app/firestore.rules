rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ¡¡REGLA DE PRUEBA!! ¡¡EXTREMADAMENTE INSEGURA!!
      // Permite que CUALQUIER persona en el mundo lea y escriba en tu base de datos.
      // Solo para diagnosticar el problema.
      allow read, write: if true;
    }
  }
}
