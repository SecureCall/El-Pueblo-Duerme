/** @type {import('next').NextConfig} */
const nextConfig = {
    // Your Next.js configuration options
    // For example:
    // images: {
    //   domains: ['example.com'],
    // },
};

export default nextConfig;
