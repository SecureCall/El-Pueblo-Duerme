name: tmp-migrate-gameplay-day-pr

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled placeholder
        run: echo 'Temporary migration runner removed.'
