rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access for assets like avatars.
      allow read;
      // Only allow authenticated users to write files.
      allow write: if request.auth != null;
    }
  }
}
