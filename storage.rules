rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read for assets like avatars
      allow read;
      // Allow authenticated users to write (e.g., upload their own avatar)
      allow write: if request.auth != null;
    }
  }
}
